<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="MaterialCalendarLight" parent="ThemeOverlay.MaterialComponents.MaterialCalendar">
        <!-- just override the colors used in the default style -->
        <item name="colorOnPrimary">@color/black</item>
        <item name="colorPrimary">@color/light_primary</item>

    </style>

    <style name="MaterialCalendarMedium" parent="ThemeOverlay.MaterialComponents.MaterialCalendar">
        <!-- just override the colors used in the default style -->
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorPrimary">@color/medium_primary</item>

    </style>

    <style name="MaterialCalendarDark" parent="ThemeOverlay.MaterialComponents.MaterialCalendar">
        <!-- just override the colors used in the default style -->
        <item name="colorOnPrimary">@color/dark_text</item>
        <item name="colorPrimary">@color/dark_primary</item>

    </style>

</resources>